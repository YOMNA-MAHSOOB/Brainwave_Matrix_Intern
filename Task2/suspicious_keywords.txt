# 🧠 Suspicious Keywords and Their Meaning

Each of the following keywords may indicate potentially harmful behavior in a script or program:

- os.system → Executes system shell commands (can be dangerous)
- subprocess → Runs external commands
- eval → Evaluates a string as code (can run arbitrary code)
- exec → Executes Python code dynamically
- pickle → Can execute arbitrary code when loading data
- base64 → Often used to hide or obfuscate payloads
- socket → Used for network connections (can be used for C2)
- urllib → Used to download data from the internet
- http:// or https:// → Indicates internet communication
- open("/dev/tcp/") → Raw socket access in shell (Linux backdoor behavior)
- powershell → Windows PowerShell command (used in many attacks)

These keywords are not always malicious by themselves, but they are **common indicators** of risky behavior when found in suspicious contexts.